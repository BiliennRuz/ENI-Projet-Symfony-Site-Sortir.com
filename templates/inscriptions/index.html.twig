<!DOCTYPE html>

<title>Inscriptions index</title>

{% block body %}
    <h1>Inscriptions index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>DateInscription</th>
                <th>Sortie</th>
                <th>Participant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for inscription in inscriptions %}
            <tr>
                <td>{{ inscription.dateInscription ? inscription.dateInscription|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ inscription.sortiesNoSortie }}</td>
                <td>{{ inscription.participantsNoParticipant }}</td>
                <td>
                    <a href="{{ path('app_inscriptions_show', {'sortiesNoSortie':inscription.sortiesNoSortie}) }}">show</a>
                    <a href="{{ path('app_inscriptions_edit', {'sortiesNoSortie':inscription.sortiesNoSortie}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_inscriptions_new') }}">Create new</a>
{% endblock %}
