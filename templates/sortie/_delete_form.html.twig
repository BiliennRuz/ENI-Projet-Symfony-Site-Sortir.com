<form method="post" action="{{ path('app_sortie_delete', {'noSortie': sorty.noSortie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sorty.noSortie) }}">
    <button class="button warning">Supprimer la sortie</button>
</form>
