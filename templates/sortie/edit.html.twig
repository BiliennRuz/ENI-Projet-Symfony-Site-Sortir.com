{% extends 'layouts/base.html.twig' %}

{% block title %}Modifier Sorties{% endblock %}

{% block body %}
    <h1>Modifier une sortie</h1>

    {{ include('sortie/_form.html.twig', {'button_label': ' '}) }} 

    {# <a href="{{ path('app_sortie_delete', {'noSortie': sorty.noSortie}) }}" class="action-button second alert"><span  class="mif-bin"></span></a> #}
    {{ include('sortie/_delete_form.html.twig') }}
    
    {# <a href="{{ path('app_sortie_index') }}" class="button alert mif-floppy-disk"></a> #}
    
{% endblock %}
